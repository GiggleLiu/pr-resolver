# OpenCode Round-Trip Test

1. Create a file `test-output.txt` in the repo root with content:
```
OpenCode + Kimi round-trip test successful!
```

2. Commit with message "Add test-output.txt from OpenCode round-trip"
