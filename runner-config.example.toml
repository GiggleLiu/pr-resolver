# GitHub Actions Runner Configuration
# Copy to runner-config.toml and edit for your setup

[runner]
base_dir = "~/actions-runners"
runner_version = "2.321.0"

# Add repos to manage (one runner per repo)
[[repos]]
repo = "your-org/repo1"

[[repos]]
repo = "your-org/repo2"
